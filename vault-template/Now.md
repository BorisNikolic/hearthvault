---
tags: [meta, hot-cache]
---

# Now — Hot Cache (index + cross-cutting)

> Per-topic hot caches live in `Now/<topic>.md` — write there for topic work so parallel sessions don't collide. This file holds only the index + cross-cutting decisions/tooling. **Overwrite, don't append**; "Prior:" chain max 3 entries; target ≤10KB for this file, ~5KB per topic cache. At session start the hook injects this file + only `Now/` caches touched in the last 14 days — older ones are one Read away. Done topics → `Now/Done/`.

**Last updated:** YYYY-MM-DD HH:MM — vault initialized.

## Active workstreams (per-topic caches)

- [[_general]] — cross-cutting blockers/threads not tied to one topic

## Done — archived to `Now/Done/` (durable one-liners)

## Cross-cutting decisions / findings

## Tooling notes
